# Domain — used by Caddy for routing
DOMAIN=pypx.app

# GitHub API token (optional) — increases rate limit from 60/hr to 5000/hr
# Create at: https://github.com/settings/tokens (no scopes needed, public repo access only)
GITHUB_TOKEN=
